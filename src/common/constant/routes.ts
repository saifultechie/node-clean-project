export enum TodoUrl {
    root = '/api/v1/todo'
}

export enum UserUrl {
    root = '/api/v1/auth',
    signin = '/signin',
    signup = '/signup',
    logout = '/logout',
    isValidSession = "/isValidSession",
}